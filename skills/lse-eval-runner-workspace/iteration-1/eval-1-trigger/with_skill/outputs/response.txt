Got it — starting the pre-run checklist before we kick off the first test.

You've confirmed:
  [x] Tool version: v1.5.4 (openwebui-tool-v1.5.4.py installed in OpenWebUI)
  [x] Prompt version: v0.5 set as system prompt
  [x] LSE Routing Filter v1.1.0 enabled
  [x] LSE Context Monitor Filter v1.0.0 enabled

Still need to verify three more items before we run test S1:

5. llama-server — is Qwen3.6-27B-Q5_K_M loaded and healthy?
   Run: curl -s localhost:8080/health
   Expected: {"status":"ok"}

6. SearxNG — is it up on port 8088? (Required for W2.)
   Run: curl -s localhost:8088
   Expected: returns HTML (any response is fine, just needs to be up)

7. Clean conversation — please open a fresh chat in OpenWebUI (no prior messages in the thread).

Once you confirm those three, I'll walk you through the first test: S1.

---

For reference, here's the run order we'll follow once we start:

  S1 → S2 → S3 → S4 → S5
  P1 → P2 → P3 → P4 → P5
  W1 → W2 → W3
  M1 → M2 → M3
  A1 (6-turn, single conversation) → A2 (fresh) → A3 (fresh)

19 tests total, scored 0–3 each, max 57 points.
Score sheet will use: Model: Qwen3.6-27B  Quant: Q5_K_M  Prompt: v0.5  Tool: v1.5.4  Filter: v1.1.0 + v1.0.0
