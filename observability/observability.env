# SINGLE SOURCE OF TRUTH — SearxNG observability (P22, 2026-06-12)
# Every config that needs the metrics token derives from THIS file via
# deploy-observability.sh. Never write the token anywhere else by hand.
# Verified live 2026-06-12: docker exec searxng grep open_metrics /etc/searxng/settings.yml

METRICS_TOKEN="JZVeoVch20+FvyjXEn4BMVHtu1AM6JCH"

# Real paths (verified via docker inspect — NOT /home/sy5/searxng-docker, which no longer exists)
SEARXNG_DATA_DIR="/home/sy5/docker/searxng_data"

# Verified 2026-06-12 via docker inspect (token curl=200, shared net docker_searxng_net):
PROMETHEUS_CONFIG="/home/sy5/docker/prometheus/prometheus.yml"
SCRAPE_TARGET="searxng:8080"   # container-internal — prometheus + searxng share docker_searxng_net
