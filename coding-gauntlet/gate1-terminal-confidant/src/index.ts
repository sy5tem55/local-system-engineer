// src/index.ts — AGENT IMPLEMENTS the entry. `npm start` launches the Ink TUI.
// Parse flags (--url, --room, --log), render <App/> with Ink, wire Ctrl-C to a
// clean exit, and enforce the give-up budget on the stream (max-tokens/max-time
// via AbortController passed to streamChat).
throw new Error("NOT IMPLEMENTED: Gate 1 entry (Ink TUI bootstrap). See SPEC.md.");
